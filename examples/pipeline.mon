fn square(x: i64) -> i64: x * x

fn min(x: i64, y: i64) -> i64: if x < y: x else: y

fn line(m: i64, x: i64, b: i64) -> i64: m * x + b

fn start() -> i64:
  5
  |> square
  |> min(20)
  |> line(10, _, 3)
